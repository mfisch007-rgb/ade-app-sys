class VectorSearchEngine { constructor() { this.index = new Map(); } storeVector(id, vector, metadata = {}) { this.index.set(id, { vector, metadata }); } cosineSimilarity(a, b) { if (a.length !== b.length) return 0; let dot = 0, normA = 0, normB = 0; for (let i = 0; i < a.length; i++) { dot += a[i] * b[i]; normA += a[i] * a[i]; normB += b[i] * b[i]; } return normA && normB ? dot / (Math.sqrt(normA) * Math.sqrt(normB)) : 0; } search(queryVector, topK = 3) { const results = []; for (const [id, data] of this.index.entries()) { const score = this.cosineSimilarity(queryVector, data.vector); results.push({ id, score, metadata: data.metadata }); } return results.sort((a, b) => b.score - a.score).slice(0, topK); } } module.exports = VectorSearchEngine;
