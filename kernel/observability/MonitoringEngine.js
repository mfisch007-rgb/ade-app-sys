class MonitoringEngine { constructor() { this.metrics = []; this.alerts = []; } recordMetric(name, value, tags = {}) { this.metrics.push({ name, value, tags, timestamp: Date.now() }); } triggerAlert(level, message) { this.alerts.push({ level, message, timestamp: Date.now() }); console.warn("[MONITORING ALERT] " + level + ": " + message); } getMetrics() { return this.metrics; } } module.exports = MonitoringEngine;
