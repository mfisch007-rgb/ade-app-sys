class ScopeEngine { constructor() { this.roleScopes = new Map([["PLATFORM_OWNER", ["*"]], ["ORG_OWNER", ["org:*", "tenant:*"]], ["ADMIN", ["tenant:read", "tenant:write"]], ["AI_AGENT", ["event:publish", "event:consume"]]]); } hasScope(role, scope) { const scopes = this.roleScopes.get(role) || []; return scopes.includes("*") || scopes.includes(scope); } } module.exports = ScopeEngine;
