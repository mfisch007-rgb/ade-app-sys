class MetricsEngine { constructor() { this.metrics = new Map(); } recordGauge(key, value) { this.metrics.set(key, { value, timestamp: Date.now() }); } incrementCounter(key, delta = 1) { const current = this.metrics.get(key)?.value || 0; this.metrics.set(key, { value: current + delta, timestamp: Date.now() }); } getSnapshot() { const snapshot = {}; for (const [k, v] of this.metrics) { snapshot[k] = v; } return snapshot; } } module.exports = MetricsEngine;
