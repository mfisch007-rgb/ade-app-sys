class KnowledgeGraphEngine { constructor() { this.nodes = new Map(); this.edges = []; } addNode(id, label, properties = {}) { this.nodes.set(id, { id, label, properties }); } addEdge(sourceId, targetId, relation) { this.edges.push({ sourceId, targetId, relation }); } queryContext(nodeId) { return { node: this.nodes.get(nodeId), connectedEdges: this.edges.filter(e => e.sourceId === nodeId || e.targetId === nodeId) }; } } module.exports = KnowledgeGraphEngine;
