const TenantEngine = require("../../kernel/identity/TenantEngine"); const CertificationValidator = require("../../kernel/manifest/CertificationValidator"); class DeveloperPortalSDK { constructor() { this.tenants = new TenantEngine(); this.certifier = new CertificationValidator(); } registerPartner(orgName, apiKey) { const tenantId = "org-" + Date.now(); this.tenants.registerTenant(tenantId, [apiKey]); return { tenantId, status: "REGISTERED" }; } certifyAdapter(manifest) { return this.certifier.certify(manifest); } } module.exports = DeveloperPortalSDK;
