class FounderToolkitEngine { constructor() { this.assessments = new Map(); } runAssessment(startupId, metrics = {}) { const techScore = metrics.techScore || 50; const marketScore = metrics.marketScore || 50; const teamScore = metrics.teamScore || 50; const founderScore = Math.round((techScore * 0.4) + (marketScore * 0.35) + (teamScore * 0.25)); const result = { startupId, founderScore, status: founderScore >= 70 ? "INVESTOR_READY" : "NEEDS_INCUBATION", recommendations: founderScore < 70 ? ["Refine pitch deck", "Strengthen technical architecture"] : ["Begin VC outreach"], timestamp: Date.now() }; this.assessments.set(startupId, result); return result; } } module.exports = FounderToolkitEngine;
