class MarketplaceEngine { constructor() { this.plugins = new Map(); } registerPlugin(name, version, author, price) { const id = "pkg_" + Date.now(); const item = { id, name, version, author, price, rating: 5.0, status: "PUBLISHED" }; this.plugins.set(id, item); return item; } getCatalog() { return Array.from(this.plugins.values()); } } module.exports = MarketplaceEngine;
