class EnterpriseMarketplace { constructor() { this.listings = new Map(); } publishExtension(id, vendorId, type, price, manifest) { const listing = { id, vendorId, type, price, manifest, verified: true, key: "lic_" + Math.random().toString(36).substring(2, 10), publishedAt: Date.now() }; this.listings.set(id, listing); return listing; } getListings(type = null) { const all = Array.from(this.listings.values()); return type ? all.filter(l => l.type === type) : all; } } module.exports = EnterpriseMarketplace;
