class PaymentGatewayEngine { constructor() { this.gateways = new Map([["stripe", { currency: "USD", active: true }], ["paystack", { currency: "NGN", active: true }], ["flutterwave", { currency: "KES", active: true }]]); } processPayment(provider, amount, currency, accountDetails) { const gw = this.gateways.get(provider.toLowerCase()); if (!gw) throw new Error("UNSUPPORTED_PAYMENT_PROVIDER"); const txId = "tx_" + provider + "_" + Date.now(); return { transactionId: txId, provider, amount, currency, status: "SUCCESS", timestamp: Date.now() }; } } module.exports = PaymentGatewayEngine;
