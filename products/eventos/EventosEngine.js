class EventosEngine { constructor() { this.events = new Map(); } createEvent(title, date, venue) { const eventId = "evt_" + Date.now(); const event = { eventId, title, date, venue, attendees: [] }; this.events.set(eventId, event); return event; } issueTicket(eventId, userId) { const ticketId = "tkt_" + Math.random().toString(36).substring(2, 8).toUpperCase(); return { ticketId, eventId, userId, qrCode: "qr_" + ticketId, issuedAt: Date.now() }; } } module.exports = EventosEngine;
