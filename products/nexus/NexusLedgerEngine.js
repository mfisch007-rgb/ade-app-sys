class NexusLedgerEngine { constructor() { this.entries = []; } recordEntry(accountFrom, accountTo, amount, memo = "") { const transaction = { txId: "tx_" + Date.now(), accountFrom, accountTo, amount, memo, timestamp: Date.now() }; this.entries.push(transaction); return transaction; } getBalance(account) { return this.entries.reduce((acc, tx) => { if (tx.accountTo === account) return acc + tx.amount; if (tx.accountFrom === account) return acc - tx.amount; return acc; }, 0); } } module.exports = NexusLedgerEngine;
