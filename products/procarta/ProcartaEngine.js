class ProcartaEngine { constructor() { this.metrics = new Map(); } trackKPI(kpiId, value, slaThreshold) { const status = value >= slaThreshold ? "COMPLIANT" : "BREACHED"; const entry = { kpiId, value, slaThreshold, status, timestamp: Date.now() }; this.metrics.set(kpiId, entry); return entry; } getHeatmap() { return Array.from(this.metrics.values()); } } module.exports = ProcartaEngine;
